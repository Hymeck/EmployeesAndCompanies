@model IEnumerable<EmployeeDto>
@{
    ViewBag.Title = "Работники";
}

<h2>@ViewBag.Title</h2>
<div class="row">
    <div class="col-md-12">
        <div>
            <form method="get" asp-action="Add">
                <button class="btn btn-success">Добавить</button>
            </form>
        </div>
        <table class="table">
            <tr>
                <th>Идентификатор</th>
                <th>Имя</th>
                <th>Фамилия</th>
                <th>Отчество</th>
                <th>Дата приема на работу</th>
                <th></th> @* for editing and deleting *@
            </tr>
            @foreach (var e in Model)
            {
                <tr>
                    <td>@e.Id</td>
                    <td>@e.Name1</td>
                    <td>@e.Name2</td>
                    <td>@e.Name3</td>
                    <td>@e.EmploymentDate.ToShortDateString()</td>
                    <td>
                        <form method="get" asp-action="Edit" asp-route-id="@e.Id">
                            <button class="btn btn-info">Изменить</button>
                        </form>


                        <form method="post" asp-route-id="@e.Id" asp-action="Delete">
                            <button class="btn btn-danger">Удалить</button>
                        </form>
                    </td>
                </tr>
            }
        </table>
    </div>
</div>